[package]
name = "kleos-cli"
version = "0.1.0"
edition = "2021"
description = "CLI client for Kleos -- persistent semantic memory for AI agents"
license-file = "../LICENSE"
repository = "https://github.com/Ghost-Frame/Kleos-rust"
homepage = "https://kleos.lol"
readme = "../README.md"

[dependencies]
kleos-lib = { path = "../kleos-lib", version = "0.1.0" }
clap = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
reqwest = { workspace = true }
rpassword = "7"
